<p><?= $post->getContent()?></p>
<form method="post" action="<?= App::config('url') ?>Index/deletePost/<?= $post->getId() ?>">

    <button type="submit">Delete</button>
</form>

<?php
arsort($comments);

foreach ($comments as $comment):
    ?>
    <p><?php echo $comment->getContent(); ?>   </p>

<?php endforeach; ?>